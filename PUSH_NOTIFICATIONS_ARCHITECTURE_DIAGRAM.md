# 📐 Firebase Push Notifications - Architecture Diagram

```
┌──────────────────────────────────────────────────────────────────────────────┐
│                          SmartClass24 Push Notifications                      │
│                          Complete System Architecture                          │
└──────────────────────────────────────────────────────────────────────────────┘


┌─────────────────────────────────────────────────────────────────────────────┐
│ 1. USER SIGN-IN & INITIALIZATION                                            │
└─────────────────────────────────────────────────────────────────────────────┘

    User Opens App
         │
         ▼
   ┌──────────────┐
   │ Auth Service │
   └──────┬───────┘
          │
          ▼
   ┌───────────────────┐
   │ Firebase Provider │ ◄── src/firebase/provider.tsx
   │ Initializes FCM   │     (Auto-initializes FCM on auth)
   └─────────┬─────────┘
             │
             ▼
   ┌────────────────────────┐
   │ messaging.ts           │ ◄── src/firebase/messaging.ts
   │ initializeMessaging()  │     (Check browser support)
   └────────────┬───────────┘
                │
                ▼
   ┌────────────────────────┐
   │ fcm-token.ts           │ ◄── src/firebase/fcm-token.ts
   │ initializeFCMToken()   │     (Get/store token)
   └────────────┬───────────┘
                │
                ▼
   ┌────────────────────────┐
   │ Request Permission     │ ◄── NotificationPermissionPrompt.tsx
   │ (If not granted)       │     (Shows after 5 seconds)
   └────────────┬───────────┘
                │
                ▼
   ┌────────────────────────┐
   │ Generate FCM Token     │
   └────────────┬───────────┘
                │
                ▼
   ┌────────────────────────────────┐
   │ Firestore:                     │
   │ users/{uid}/fcmTokens/{token}  │
   │ ✅ Token saved                 │
   └────────────────────────────────┘


┌─────────────────────────────────────────────────────────────────────────────┐
│ 2. SENDING NOTIFICATIONS (Client → Cloud Function → FCM)                    │
└─────────────────────────────────────────────────────────────────────────────┘

    User Creates Challenge
           │
           ▼
   ┌────────────────────────┐
   │ arena-notifications.ts │ ◄── src/lib/arena-notifications.ts
   │ sendArenaNotification  │     (Client-side helper)
   │ ViaFirestore()         │
   └────────────┬───────────┘
                │
                ▼
   ┌────────────────────────────────────┐
   │ Firestore Document Created:        │
   │ users/{opponentUid}/               │
   │   notifications/{notifId}          │
   │   {                                │
   │     type: 'arena_challenge_invite',│
   │     challengerName: 'John',        │
   │     challengeId: 'abc123',         │
   │     pendingPush: true  ◄───────────┼─── Triggers Cloud Function
   │   }                                │
   └────────────┬───────────────────────┘
                │
                ▼
   ┌────────────────────────────────┐
   │ Firebase Cloud Function        │ ◄── functions-example.ts
   │ sendArenaPushNotification()    │     (Deploy with firebase deploy)
   │                                │
   │ 1. Firestore onCreate trigger  │
   │ 2. Get user's FCM tokens       │
   │ 3. Build notification payload  │
   │ 4. Send via Admin SDK          │
   │ 5. Mark as sent                │
   └────────────┬───────────────────┘
                │
                ▼
   ┌────────────────────────────────┐
   │ Firebase Cloud Messaging (FCM) │
   │ Google's Push Service          │
   └────────────┬───────────────────┘
                │
                ├─────────────┬─────────────┐
                │             │             │
                ▼             ▼             ▼
         [Device 1]    [Device 2]    [Device 3]


┌─────────────────────────────────────────────────────────────────────────────┐
│ 3. RECEIVING NOTIFICATIONS (FCM → Service Worker/App → User)                │
└─────────────────────────────────────────────────────────────────────────────┘

    FCM Delivers Notification
           │
           │
    ┌──────┴──────────────────┐
    │                         │
    ▼                         ▼
┌──────────────┐      ┌───────────────┐
│ APP CLOSED   │      │ APP OPEN      │
└──────┬───────┘      └───────┬───────┘
       │                      │
       ▼                      ▼
┌───────────────────┐  ┌──────────────────────┐
│ Service Worker    │  │ NotificationHandler  │ ◄── src/components/
│ Background        │  │ Foreground           │     NotificationHandler.tsx
│                   │  │                      │
│ firebase-         │  │ setupForeground      │
│ messaging-sw.js   │  │ MessageListener()    │
│                   │  │                      │
│ Shows System      │  │ Shows Toast          │
│ Notification      │  │ Notification         │
└─────────┬─────────┘  └──────────┬───────────┘
          │                       │
          └───────────┬───────────┘
                      │
                      ▼
         ┌─────────────────────────┐
         │ User Sees Notification  │
         └────────────┬────────────┘
                      │
                      ▼
         ┌─────────────────────────┐
         │ User Clicks Notification│
         └────────────┬────────────┘
                      │
                      ▼
         ┌─────────────────────────┐
         │ Navigate to /arena      │
         │ with challengeId param  │
         └─────────────────────────┘


┌─────────────────────────────────────────────────────────────────────────────┐
│ 4. DATA STORAGE (Firestore Structure)                                       │
└─────────────────────────────────────────────────────────────────────────────┘

    Firestore Database
    │
    ├── users/
    │   │
    │   ├── {userId}/
    │   │   │
    │   │   ├── fcmTokens/              ◄── FCM Tokens (subcollection)
    │   │   │   │
    │   │   │   ├── {token1}/
    │   │   │   │   ├── token: "xyz..."
    │   │   │   │   ├── userId: "user123"
    │   │   │   │   ├── createdAt: timestamp
    │   │   │   │   ├── updatedAt: timestamp
    │   │   │   │   └── deviceInfo: {...}
    │   │   │   │
    │   │   │   └── {token2}/
    │   │   │       └── ...
    │   │   │
    │   │   └── notifications/          ◄── Notifications (subcollection)
    │   │       │
    │   │       ├── {notifId1}/
    │   │       │   ├── type: "arena_challenge_invite"
    │   │       │   ├── challengerId: "user456"
    │   │       │   ├── challengerName: "John"
    │   │       │   ├── challengeId: "challenge789"
    │   │       │   ├── pendingPush: true/false
    │   │       │   ├── sentAt: timestamp
    │   │       │   └── read: true/false
    │   │       │
    │   │       └── {notifId2}/
    │   │           └── ...
    │   │
    │   └── {userId2}/
    │       └── ...
    │
    └── challenges/
        └── ...


┌─────────────────────────────────────────────────────────────────────────────┐
│ 5. SECURITY RULES (Firestore)                                               │
└─────────────────────────────────────────────────────────────────────────────┘

    users/{userId}/fcmTokens/{tokenId}
        ├── Read:   ✅ auth.uid == userId (Owner only)
        ├── Write:  ✅ auth.uid == userId (Owner only)
        └── Delete: ✅ auth.uid == userId (Owner only)

    users/{userId}/notifications/{notifId}
        ├── Read:   ✅ auth.uid == userId (Owner only)
        ├── Create: ✅ auth != null      (Any authenticated user can create)
        ├── Update: ✅ auth.uid == userId (Owner only)
        └── Delete: ✅ auth.uid == userId (Owner only)


┌─────────────────────────────────────────────────────────────────────────────┐
│ 6. COMPONENT TREE                                                            │
└─────────────────────────────────────────────────────────────────────────────┘

    app/layout.tsx
    │
    └── FirebaseProvider                    ◄── src/firebase/provider.tsx
        │   └── initializeFCMForUser()      (Auto-initializes FCM)
        │
        ├── NotificationHandler             ◄── src/components/NotificationHandler.tsx
        │   └── setupForegroundMessageListener()
        │
        ├── NotificationPermissionPrompt    ◄── src/components/NotificationPermissionPrompt.tsx
        │   └── requestNotificationPermission()
        │
        └── {children}
            └── Your App Pages


┌─────────────────────────────────────────────────────────────────────────────┐
│ 7. FILE DEPENDENCIES                                                         │
└─────────────────────────────────────────────────────────────────────────────┘

    firebase/provider.tsx
    ├── imports messaging.ts → initializeMessaging()
    ├── imports fcm-token.ts → initializeFCMToken(), setupTokenRefreshListener()
    └── calls initializeFCMForUser()

    components/NotificationHandler.tsx
    ├── imports messaging.ts → setupForegroundMessageListener()
    ├── imports firebase → useFirebase()
    └── listens to service worker messages

    components/NotificationPermissionPrompt.tsx
    ├── imports messaging.ts → getNotificationPermission(), requestNotificationPermission()
    └── shows UI after delay

    lib/arena-notifications.ts
    ├── imports Firestore → addDoc(), collection()
    └── creates notification documents

    public/firebase-messaging-sw.js
    ├── importScripts Firebase SDK
    ├── onBackgroundMessage()
    └── notificationclick event listener


┌─────────────────────────────────────────────────────────────────────────────┐
│ 8. NOTIFICATION TYPES & ACTIONS                                              │
└─────────────────────────────────────────────────────────────────────────────┘

    arena_challenge_invite
    │
    ├── Title: "⚔️ Arena Challenge!"
    ├── Body:  "{name} challenges you to battle!"
    └── Actions:
        ├── "Accept Challenge" → /arena?accept={challengeId}
        └── "View"             → /arena

    arena_challenge_accepted
    │
    ├── Title: "🎮 Challenge Accepted!"
    ├── Body:  "{name} accepted your challenge!"
    └── Actions:
        ├── "Start Game"       → /arena?start={challengeId}
        └── "View"             → /arena

    arena_challenge_completed
    │
    ├── Title: "🏆 Battle Complete!"
    ├── Body:  "Check the results!"
    └── Action:
        └── Click              → /arena


┌─────────────────────────────────────────────────────────────────────────────┐
│ 9. TOKEN LIFECYCLE                                                           │
└─────────────────────────────────────────────────────────────────────────────┘

    ┌─────────────────┐
    │ User Signs In   │
    └────────┬────────┘
             │
             ▼
    ┌──────────────────────┐
    │ Check LocalStorage   │
    │ for existing token   │
    └────────┬─────────────┘
             │
        ┌────┴────┐
        │         │
        ▼         ▼
    ┌──────┐  ┌──────────┐
    │Found │  │Not Found │
    └───┬──┘  └────┬─────┘
        │          │
        │          ▼
        │     ┌─────────────────────┐
        │     │ Request Permission  │
        │     └──────────┬──────────┘
        │                │
        │                ▼
        │     ┌─────────────────────┐
        │     │ Generate New Token  │
        │     └──────────┬──────────┘
        │                │
        └────────┬───────┘
                 │
                 ▼
    ┌────────────────────────┐
    │ Save to Firestore      │
    │ Save to LocalStorage   │
    └────────────┬───────────┘
                 │
                 ▼
    ┌────────────────────────┐
    │ Setup Refresh Listener │
    └────────────┬───────────┘
                 │
                 ▼
    ┌────────────────────────┐
    │ Token Active           │
    │ (Receives Push)        │
    └────────────────────────┘
                 │
                 │ (90 days or error)
                 ▼
    ┌────────────────────────┐
    │ Token Expired/Invalid  │
    └────────────┬───────────┘
                 │
                 ▼
    ┌────────────────────────┐
    │ Auto Refresh Token     │
    └────────────────────────┘
                 │
                 └──────────► (Back to Save)


┌─────────────────────────────────────────────────────────────────────────────┐
│ 10. ERROR HANDLING                                                           │
└─────────────────────────────────────────────────────────────────────────────┘

    Browser Not Supported
    └─► messaging.ts: isSupported() → false → Graceful return

    Permission Denied
    └─► messaging.ts: requestNotificationPermission() → 'denied' → Show manual enable

    Token Generation Failed
    └─► fcm-token.ts: getFCMToken() → null → Log error, continue app

    Firestore Save Failed
    └─► fcm-token.ts: saveFCMToken() → catch → Keep in LocalStorage only

    Service Worker Failed
    └─► firebase-messaging-sw.js: error → Log, fallback to foreground only

    Cloud Function Failed
    └─► Update notification doc with error → Retry logic in function


┌─────────────────────────────────────────────────────────────────────────────┐
│ LEGEND                                                                       │
└─────────────────────────────────────────────────────────────────────────────┘

    ◄──  Points to file location
    │    Flow direction
    ▼    Next step
    ✅   Permission/security rule
    └─►  Alternative path or error handling
```

---

## Quick Reference

| Component | Purpose | File |
|-----------|---------|------|
| **messaging.ts** | FCM initialization | `src/firebase/messaging.ts` |
| **fcm-token.ts** | Token management | `src/firebase/fcm-token.ts` |
| **Service Worker** | Background notifications | `public/firebase-messaging-sw.js` |
| **NotificationHandler** | Foreground notifications | `src/components/NotificationHandler.tsx` |
| **PermissionPrompt** | Request permission UI | `src/components/NotificationPermissionPrompt.tsx` |
| **arena-notifications** | Send notifications | `src/lib/arena-notifications.ts` |
| **Cloud Function** | Push delivery | `functions-example.ts` |
| **Firestore Rules** | Security | `firestore.rules` |

---

**Visual diagram created by GitHub Copilot** 🎨  
**Date**: January 14, 2026
